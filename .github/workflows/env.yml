name: env try

on:
 
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello from second"

      - name: Install
        env:
          APP_ENV: demonew
        run: echo "Environment $APP_ENV"
