name: env try

on:
  workflow_dispatch:
  # push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello from second"

      - name: Install
        env:
          APP_ENV: demo
        run: echo "Environment $APP_ENV"
